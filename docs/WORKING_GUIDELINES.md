# Working Guidelines

## Form Placeholders

- Do not prefill forms with example operational values.
- Use placeholder text only as a generic hint, such as `평가 이름`.
- Do not use date-like or business-specific examples in placeholders or default form state.
- If a value has operational meaning, require the user to enter it explicitly.
