[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "evaluation-protocol-backend"
version = "0.1.0"
description = "FastAPI backend for the internal evaluation protocol service"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
include = ["app*"]
